# Kopieren nach .env – wird nur für "docker compose --profile app up" gebraucht.
# Die Datenbank allein (docker compose up -d postgres) läuft ohne .env.
#
#   cp .env.example .env
#   mix phx.gen.secret   # Ergebnis unten eintragen
SECRET_KEY_BASE=
